{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "compile": "lerna run compile",
    "clean": "lerna run clean",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "bump": "lerna version",
    "publish": "lerna publish from-git",
    "check": "yarn clean && yarn compile && yarn test && yarn lint"
  },
  "devDependencies": {
    "lerna": "^3.3.0"
  }
}
